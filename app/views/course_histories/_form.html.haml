= form_for @course_history do |f|
  - if @course_history.errors.any?
    #error_explanation
      %h2= "#{pluralize(@course_history.errors.count, "error")} prohibited this course_history from being saved:"
      %ul
        - @course_history.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
