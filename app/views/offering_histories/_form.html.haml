= form_for @offering_history do |f|
  - if @offering_history.errors.any?
    #error_explanation
      %h2= "#{pluralize(@offering_history.errors.count, "error")} prohibited this offering_history from being saved:"
      %ul
        - @offering_history.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
